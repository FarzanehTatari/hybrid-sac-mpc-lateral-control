# Pinned to the exact versions used to produce the paper results.
# For a fresh install:
#     python -m venv .venv && source .venv/bin/activate
#     pip install -r requirements.txt
casadi==3.7.2
gymnasium==1.3.0
matplotlib==3.10.9
numpy==2.2.6
osqp==1.1.3
pandas==2.3.3
PyYAML==6.0.3
scipy==1.15.3
torch==2.13.0
